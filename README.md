# net_sentinel
NetSentinel is a high-performance network audit tool built in Rust. It captures real-time metadata of TCP/UDP packets for specific IP addresses, logging critical details like Flags, TTL, and Window Size into a JSONL audit file. Engineered for low latency and minimal resource overhead, it provides essential traffic visibility for security monitoring
